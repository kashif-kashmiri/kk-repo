---
 - name: adding user
   hosts: vm2
   tasks:
    - name: adding tahir
      user:
        name: tahir
        state: present 
    - name: stoping a service
      service:
        name: firewalld
        state: stopped
  
  


